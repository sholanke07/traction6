package com.example.data.repository

import com.example.data.Network.ApiClient
import com.example.data.Network.apis.BusinessApi
import com.example.data.remote.model.NetworkResponses
import io.reactivex.Observable

class BusinessRepository {
    /*private val api: BusinessApi = ApiClient.retrofit.create(BusinessApi::class.java)
) {

    fun businessByUser(): Observable<NetworkResponses> {
        return api.businessByUser().doOnNext {
            // it.data?.let { businessData ->
            // saveBusiness(businessData)
            //  }
        }
    }*/

}